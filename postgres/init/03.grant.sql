-- Grant all privileges on schema aufa_db to user aufa_db
GRANT ALL PRIVILEGES ON SCHEMA aufa_db TO aufa_db;

-- Grant all privileges on all tables in schema aufa_db to user aufa_db
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA aufa_db TO aufa_db;

-- Grant all privileges on all sequences in schema aufa_db to user aufa_db
GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA aufa_db TO aufa_db;

-- Grant all privileges on all functions in schema aufa_db to user aufa_db
GRANT ALL PRIVILEGES ON ALL FUNCTIONS IN SCHEMA aufa_db TO aufa_db;